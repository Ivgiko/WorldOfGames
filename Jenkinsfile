pipeline {
    agent any
    stages {
        stage('Checkout Code') {
            steps {
                // Checkout the repository
                git branch: 'main', url: 'https://github.com/Ivgiko/WorldOfGames.git'
            }
        }
    }
}
